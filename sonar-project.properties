# # must be unique in a given SonarQube instance
# sonar.projectKey=vignesh

# # --- optional properties ---

# # defaults to project key
# sonar.projectName=mad
# # defaults to 'not provided'
# sonar.projectVersion=1.0
 
# # Path is relative to the sonar-project.properties file. Defaults to .
# sonar.sources=.
 
# # Encoding of the source code. Default is default system encoding
# sonar.sourceEncoding=UTF-8


# Required metadata sonar.projectKey=java-sonar-runner-simple sonar.projectName=Simple Java project analyzed with the SonarQube Runner sonar.projectVersion=1.0 # Comma-separated paths to directories with sources (required) sonar.sources=src # Language sonar.language=java # Encoding of the source files sonar.sourceEncoding=UTF-8

sonar.login=admin
sonar.password=Admin
# must be unique in a given SonarQube instance
sonar.projectKey=deployment-project

# --- optional properties ---

# defaults to project key
sonar.projectName=deployment-project
# defaults to 'not provided'
sonar.projectVersion=1.0
 
# Path is relative to the sonar-project.properties file. Defaults to .
sonar.sources=./src/services
 
sonar.exclusions=./node_modules/*, **app.js** , **/coverage/lcov-report**,./src/index.js , ./src/db/mongoose.js
#**/test/**


# Encoding of the source code. Default is default system encoding
sonar.sourceEncoding=UTF-8

sonar.javascript.lcov.reportPaths= ./coverage/lcov.info
# app/coverage/lcov.info

sonar.javascript.lcov.reportPaths=coverage/lcov.info
# sonar.javascript.lcov.reportPaths=coverage/lcov.info
sonar.test.inclusion